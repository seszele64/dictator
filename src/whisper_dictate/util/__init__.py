"""Pure utility helpers for whisper-dictate.

Modules in this package are leaf modules: they import NOTHING from
``whisper_dictate`` (roadmap S3 import-direction rule, enforced by
import-linter from Day 8), so any package module may depend on them —
never the reverse.
"""
