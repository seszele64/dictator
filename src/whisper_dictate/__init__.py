"""whisper-dictate - Voice dictation using OpenAI Whisper API."""

# Single source of truth for the version. pyproject.toml declares
# ``dynamic = ["version"]`` and hatchling reads it from here
# (``[tool.hatch.version]``), so the package and its distribution metadata
# can never drift apart.
__version__ = "0.1.0"
